#include<stdio.h>

void main()
{
    float l,b,P;
    printf("Enter length and breadth respectively: ");
    scanf("%f%f",&l,&b);
    P = 2*(l+b);
    printf("Perimeter of Rectangle = %f",P);
}


conditional Operator : greatest between two numbers
Conditionals ==> if statement, if..else, if..else if... else
nested if, switch ... case
